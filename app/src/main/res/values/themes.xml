<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ReshQMess" parent="Theme.AppCompat.DayNight.NoActionBar">

        <!-- Primary Color -->
        <item name="colorPrimary">@color/red_primary</item>
        <item name="colorPrimaryDark">@color/red_primary</item>
        <item name="colorAccent">@color/red_primary</item>

        <!-- Background -->
        <item name="android:windowBackground">@color/black_bg</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@android:color/black</item>

    </style>

</resources>